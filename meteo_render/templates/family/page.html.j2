<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Weather</title>
    <link rel="stylesheet" href="style.css">
  </head>
  <body>
    <table>
        {% for day in data %}
        <tr>
            {% for hour in day["hours"] %}
            <td class="weather-hour{% if hour["time"] == extra["hour"] %} weather-this-hour{% endif %}">
                <p>
                    <span class="logo">
                        <img src="{{ extra["images"][hour["weathercode"]] }}"/>
                    </span>
                </p>
                <p>
                    {{ extra["names"][hour["weathercode"]] }}
                </p>
                <p>
                    {{ hour["temperature_2m"] | int }}
                    {{ hour["time"][-5:] }}
                </p>
            <td>
            {% endfor %}
        <tr>
        {% endfor %}
    </table> 
  </body>
</html>
